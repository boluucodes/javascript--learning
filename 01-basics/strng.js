// Some Basic String Methods
// these are actions you are performing
// .length is a property that is already pre-computed;


"BLUE".toLowerCase(); // "blue";
"red".toUpperCase(); // "RED";

let language = "JavaScript";
language.at(0); // "J"
language.at(1); // "a"
language.at(-1); // "t"
language.at(-2); // "p"

// The .at() method allows you to read a character at an index (which can also be negative).


someString.substring(indexStart, indexEnd)
// indexStart: the position of the first character you'd like to include
// indexEnd: the position of the first character you'd like to ignore

